package com.benhadfield.query;

/**
 * The Query class is responsible for taking user a user's query, comprised of one or more terms, and returning the
 * paths to the files which contain those terms, along with the frequency of the term in each document.
 */

public class Query {

}
